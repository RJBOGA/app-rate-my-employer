# Copy to .env.local for local development. Both values are PUBLIC by design:
# the publishable key is safe in a browser because every table is protected
# by Row Level Security and every privileged operation is a SECURITY DEFINER
# function that checks the caller's role.
#
# NEVER put a service-role / sb_secret_ key here. src/lib/env.ts refuses to
# boot if it detects one.

VITE_SUPABASE_URL=https://ccdtebczdoaanpcpwklx.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_XSL3uJmlPeeNu23ajTOEuA_ryror19c
